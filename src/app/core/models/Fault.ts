export interface FaultType {
    id: number;
    name: string;
    description: string
}

export interface FaultTypeAPI{
    tipoAveriaId: number;
    tipoNombre: string;
    tipoDescripcion: string;
}